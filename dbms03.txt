1) Select 구문 : DB에 저장되어 있는 자원들을 검색할 때 사용

--사번, 이름, 직종, 급여, 보너스, 보너스의 실제 금액을 조회
select employee_id,
	 first_name,
	 job_id,
	 salary,
	 commission_pct,
	 salary * commission_pct bonus
from employees;

--distinct : 중복을 제거하는 명령어
select distinct JOB_ID
from employees;

--사원중에 관리자(매니저)로써 활동중인 사원이 
--몇 명인지 사번 출력해보기.
select distinct manager_id
from employees;